
<ul class="menu">
    {{#each items}}
        <li>
            <div class="menu_item">
                <img src="{{this.picture}}" alt="">
                <div class="menu_item_text">
                    <label>{{this.name}}</label> 
                    <input type="button" value="afficher" class="id_input" data-id={{this.id_cake}}>
                </div>
                
            </div>
        </li>
    {{/each}}
</ul>
